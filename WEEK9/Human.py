

class Human():
    #Class attribute
    MAX_ENERGY = 100

    def __init__(self):
        self.name = "Human"
        self.age = 0
        self.energy = self.MAX_ENERGY


    def display(self):
        print(f"I am {self.name}")

        def grow(self):
            self.age += 1

        def eat(self, amount):
            total_energy = self.energy + amount
            if total_energy <= 100
                self.energy += amount

        def move(self.)

if(__name__ == "__main__"):
    human = Human()
    human.display()

